import GenderContext, { 
  GenderProvider, 
  useGender, 
  CHILD_IMAGES, 
  GENDER_THEMES 
} from './GenderContext';

export { GenderProvider, useGender, CHILD_IMAGES, GENDER_THEMES };
export type { Gender } from './GenderContext';
export default GenderContext;

